{
  "name": "golf-charity-platform",
  "private": true,
  "packageManager": "npm@10.2.4",
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "turbo run dev & sleep 6 && echo '' && echo '━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━' && echo '  ⛳  Golf Charity Platform — Running!' && echo '' && echo '  📱  Frontend :  http://localhost:3000' && echo '  🔌  API       :  http://localhost:3001/api' && echo '  📚  API Docs  :  http://localhost:3001/api/docs' && echo '━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━' && echo '' && wait",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:generate": "turbo run db:generate",
    "db:push": "turbo run db:push",
    "db:migrate": "turbo run db:migrate",
    "db:seed": "turbo run db:seed",
    "docker:up": "docker compose -f docker-compose.yml up -d",
    "docker:down": "docker compose -f docker-compose.yml down",
    "docker:logs": "docker compose -f docker-compose.yml logs -f"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
